Given an integer N, determine whether it is equal to the factorial of some non-negative integer x (i.e., N=x! for some x ≥ 0). 
If yes, print that number whose factorial is equal to N, otherwise print "NO".
#include <stdio.h>

int main() {
      int n;
    scanf("%d",&n);
    int sum=1;
    for(int i=1;i<=10000;i++){
        sum=sum*i;
        if(n==sum){
           printf("%d",i); 
            return 0;
        }
    }
         printf("NO");   
}
